.comments-form 
  =form_for [commentable, commentable.comments.new], remote: true do |f|
    =f.label :body, 'Comment text'
    .commentable-comment-errors
    =f.text_area :body, row: 15, class: "form-control", id: "commentable-text-form-#{commentable.id}"
    .actions
      =f.submit
